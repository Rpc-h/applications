apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: latency-monitor
  namespace: production
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: production-latency-monitor
  endpoints:
    - port: "http"
      interval: 30s
      relabelings:
        #Expose discovery stage meta labels as real labels
        - sourceLabels: [ __meta_kubernetes_pod_container_image ]
          regex: (.*)
          targetLabel: image
          action: replace